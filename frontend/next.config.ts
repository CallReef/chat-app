import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Enable component caching for better performance (Next.js 16 feature)
  cacheComponents: true,
};

export default nextConfig;
